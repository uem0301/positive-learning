<h1>検索結果</h1>
<div class= "search_results">
  <div class= "content">
    <% if @search_items.present?%>
      <% @search_items.each do |search_item| %>
        <div class= "content_box">
          <%= link_to item_path(search_item.id), method: :get do %>
            <p> <%= image_tag search_item.image.url, class:"content__image", height: "100%", width: "80%" %> </p>
            <p> <%= video_tag search_item.video.url, type: "video/mp4", autoplay: true, muted: true, controls: true, preload: true, class:"content__video", height: "20%", width: "20%" unless search_item.video.blank? %></p>
            <div class= "content__explanation">
              <p><%= search_item.explanation %> </p>
            </div>
          <% end %>
        </div>
      <% end %>
    <% else %>
      <%= link_to "お探しの投稿はありませんでした。TOPページへ戻る", '/', class: "back" %>
    <% end %>
  </div>
</div>